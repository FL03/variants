version: 2

updates:
  - package-ecosystem: cargo
    schedule:
      interval: monthly
    directories:
      - /
      - /variants
      - /derive
      - /macros
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
